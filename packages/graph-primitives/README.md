# @graph/primitives

Foundational building blocks for the entire graph system. Defines `CoreNode`, `CoreEdge`, transaction types, and the action/getter interfaces that core and plugins build on. Events live in `@core/events`, which nothing here depends on.
